{
  "name": "transrota",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "setup": "node setup.js",
    "predev": "node -e \"const fs=require('fs'); if(!fs.existsSync('node_modules')||!fs.existsSync('apps/api/node_modules')||!fs.existsSync('apps/web/node_modules')){console.log('\\n📦 Dependências faltando — executando pnpm install...\\n'); require('child_process').execSync('pnpm install', {stdio:'inherit'})}\"",
    "prebuild": "node -e \"const fs=require('fs'); if(!fs.existsSync('node_modules')||!fs.existsSync('apps/api/node_modules')||!fs.existsSync('apps/web/node_modules')){console.log('\\n📦 Dependências faltando — executando pnpm install...\\n'); require('child_process').execSync('pnpm install', {stdio:'inherit'})}\"",
    "infra": "docker compose -f docker/docker-compose.dev.yml up -d",
    "infra:down": "docker compose -f docker/docker-compose.dev.yml down",
    "dev": "pnpm run infra && turbo run dev",
    "dev:api": "turbo run dev --filter=@transrota/api",
    "dev:web": "turbo run dev --filter=@transrota/web",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.0",
    "prettier": "^3.8.2",
    "turbo": "^2.1.2",
    "typescript": "^5.5.4"
  },
  "pnpm": {
    "overrides": {
      "multer": ">=2.1.1"
    }
  },
  "overrides": {
    "multer": ">=2.1.1",
    "next": "14.2.35"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
